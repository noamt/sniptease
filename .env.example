# SnipTease environment variables
# Copy this file to `.env` and fill in your values, or export them in your shell.

# Gemini API key — used by the MCP agent_capture tool and the Python test scripts
# in agent/ to ask the VLM where to frame the shot. Get one free at:
#   https://aistudio.google.com/apikey
GEMINI_API_KEY=
